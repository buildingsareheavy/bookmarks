info:
  name: GET bookmarks
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/bookmarks
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
